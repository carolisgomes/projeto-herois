### Olá eu sou a Caroline! 🎀

Esse repertório serve para representar meu trabalho com o projeto proposto no Bootcamp da Santander de Fundamentos de Lógica de Programação.

---

### Informações adicionais 

Seguindo esse raciocínio, irei deixar algumas informações que coletei durante meus estudos no Bootcamp.

---

### Base sobre algoritmos 📝

Lógica de Programação -> Estruturar o pensamento de uma maneira lógica para escrever algoritmos.
Algoritmos -> Passo a passo lógivo do que deve ser executado para resolver um problema.
Fluxograma -> Representação visual de uma sequência de ações lógicas.

---

### Estrutura de um Software e seu ambiente de desenvolvimento 

Input -> Momentos de interação com o usuário.
Process -> Verificações e ações para realizar um processo.
Output -> Comandos de saída, para retornar algo ao usuário.

Cada aplicação tem inputs, process e outputs diferentes, porém todas as aplicações contam com esses 3 modelos. Uma única aplicação contém vários desses momentos.

Feature -> Termo em inglês que significa funcionalidade, referindo-se a uma parte específica do software que entrega valor ou uma capacidade útil para o usuário final.

---

### Trabalhando com variáveis 🌐

Variáveis -> É um espaço na memória do computador, nomeado para armazenar um valor que pode ser alterado. Funciona como uma caixinha para guardar e manipular dados, como números e textos, durante a execução de um programa.

Diferença de uma constante pra uma variável -> A constante é um número que não pode ser mudado, deve ser usado para um valor repetitivo. As variáveis podem ser alteradas.

---

### Tipos de variáveis 

String -> São variáveis que armazenam texto.
Number -> Variáveis que armazenam números.
Boolean -> Variáveis lógicas, que armazenam o valor de ligado ou desligado (true ou false).

---

### Vetores e Matrizes📝

Vetores -> É uma estrutura de dados que armazena uma coleção de valores do mesmo tipo em uma única variável. É como um armário armazenando os valores.

Variáveis, vetores e matrizes são uma maneira de capturar informações do usuário ou até de outras ferramentas, guardar em algum lugar e levar pra outro de uma forma mais organizada.

Matrizes -> É uma estrutura de dados que armazena uma coleção de elementos do mesmo tipo (número ou strings) sob um único nome de variável. Ela organiza dados em uma estrutura retangular, como uma tabela com linhas e colunas, sendo acessados por um ou mais indices.

---

### Estruturas condicionais (if, else if, else)

Estruturas condicionais em programação são instruções de decisão que permitem que um programa execute diferentes blocos de códigos com base em condições específicas serem verdadeiras ou falsas. Elas controlam o fluxo de execuções, possibilitando que o programa "pense" e tome decisões lógicas, como verificar se uma condição é true ou false para decidir qual caminho seguir.

If -> Verifica uma condição. Se a condição for true, o código dentro do if é executado.
Else if -> É uma continuação do If. Ele só é verificado se a condição do if anterior for false.
Else -> É o "plano de fuga". Ele é executado se nenhuma das condições if ou else if anteriores for verdade (true).
